import {Color} from '../../../utils/color';
const tripStatusProps = {
  statusObj: {
    type: 'IN_SHORTLISTING',
    text: 'Approval Pending',
    color: Color.MANGO,
  },
};

export default tripStatusProps;
